name: CI

on:
  push:
    branches: main
  pull_request:

jobs:
  prettier:
    runs-on: ubuntu:latest
    steps:
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts
      - name: Install Dependencies
        run: npx prettier --check .
